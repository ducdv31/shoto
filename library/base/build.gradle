apply plugin : 'com.android.library'
apply from : '../../common.gradle'

android {

    defaultConfig {
        consumerProguardFiles 'proguard-base.pro'
    }

    sourceSets {
        main {
            res.srcDirs(
                    'src/main/res',
                    'src/main/res-sw',
            )
        }
    }
}